#ifndef GIZMO_AXIS_HPP
#define GIZMO_AXIS_HPP

/**
 * @brief Axis handle of the transform gizmo currently hovered / grabbed.
 */
enum class GizmoAxis
{
    NONE = -1,
    X = 0,
    Y = 1,
    Z = 2
};

#endif // GIZMO_AXIS_HPP
